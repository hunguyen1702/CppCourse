//Check the input day valid or not, if valid check that year is leap or not

//Check leap year
bool isLeapYear(unsigned int year);

//Check valid day
bool isValidDay(unsigned int day, unsigned int month, unsigned int year);