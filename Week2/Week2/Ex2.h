#include <iostream>
#include <vector>
#include <string>

using namespace std;

//search for key word in a text by column and row
void searchWord(const string, const string, vector<int> &, vector<int> &);

string replaceWord(const string, const string, vector<int> &, vector<int> &);